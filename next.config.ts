import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable the Next.js dev overlay build indicator (the "N" circle in bottom-left)
  devIndicators: false,
};

export default nextConfig;
